# nielsgl Homebrew Tap

Homebrew tap for `ResetPing` and future packages.

## Install tap

```bash
brew tap nielsgl/tap
```

## Install casks

```bash
brew install --cask nielsgl/tap/resetping
```

`Casks/resetping.rb` is managed by the `resetping` release workflow and is updated automatically after each GitHub release.
